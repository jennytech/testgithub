<p id="notice"><%= notice %></p>

<!-- <div class="row">
  <div class="col-xs-12 col-lg-4">
     <%= image_tag @product.image_url,class: "img-responsive", width: "840"  %>
     
  </div> 
</div> !-->


<div class="row">
    <div class="col-xs-12 col-md-4">
      <%= image_tag(@product.image_url, data_zoom_image: "@product.image_url", width: "350", id: "img-zoom") %>
    </div>

    <div class="col-xs-12 col-md-8">
      <h1><%= @product.name %></h1>
      <p><%= @product.description %></p>
     
      <p>Average Rating: <div class="rated" data-score="<%= @product.average_rating %>"></div></p>
      <p>
      <%= form_tag "/payments/create" do %>
      <%= hidden_field_tag :product_id, @product.id %>
      <%= render partial: "shared/stripe_checkout_button" %>
      <% end %>
      </p>
    </div>

  </div>











<%= link_to 'Edit', edit_product_path(@product), class: "btn btn-default" %> |
<%= link_to 'Back', products_path, class: "btn btn-default" %>
</div>
 


<%= render 'products/new_comment' %>
<%= render 'comments' %>

<p>Average Rating: <div class="rated" data-score="<%= @product.average_rating %>"></div> </p>

<div class="digg_pagination">
    <div class="page_info">
      <%= page_entries_info @comments %>
    </div>
    <div class="comment" style="margin-top: 30px">
    <%= will_paginate @comments, :container => false  %>
  </div>
</div>

